
#include "parser.hh"

struct parse_result {
	int split;
	TokenType type;
};


parse_result tokenizeLambdaDecl(const char * str, int len)
{

}

vector<Token> parse_tokens(const str text)
{
	for (int i = 0; i < text.length; i++) {
		
	}

}
