der(BindPriv_add_0) { };
der(BindPriv_add_1) { };
der(Bind_add) {
};

der(Bind_ec) {
	int counter;
};

der(Bind_error) {
	Bind_error() { Init_Bind_error(this); } 
} Instance_Bind_error;

der(Bind_facc) {
};

der(Bind_print_false) {
};

der(Bind_print_true) {
};

